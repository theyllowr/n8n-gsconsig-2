name: Build and Push n8n Custom to GCR

on:
  push:
    branches:
      - main
  workflow_dispatch:  # Permite executar manualmente
    inputs:
      n8n_version:
        description: 'N8N version to build (e.g., latest, 1.65.2)'
        required: false
        default: 'latest'

env:
  GCP_PROJECT_ID: n8ngsconsig
  GCR_HOSTNAME: gcr.io
  IMAGE_NAME: n8n-bypassed

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      id-token: write

    steps:
      - name: 📥 Checkout n8n-bypass repository
        uses: actions/checkout@v4

      - name: 🔍 Determine N8N version
        run: |
          N8N_VERSION="${{ github.event.inputs.n8n_version || 'latest' }}"
          echo "N8N_VERSION=$N8N_VERSION" >> $GITHUB_ENV
          echo "✅ Will build n8n version: $N8N_VERSION"

      - name: 🔐 Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: 🛠️ Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: 🔧 Configure Docker for GCR
        run: |
          gcloud auth configure-docker gcr.io

      - name: 📝 Create patch script
        run: |
          mkdir -p /tmp/n8n-custom
          cat > /tmp/n8n-custom/apply-patches.sh << 'PATCH_EOF'
          #!/bin/sh
          set -e
          
          echo "🩹 Applying license bypass patches..."
          
          # Patch 1: license.js - Método isLicensed (bypass global)
          LICENSE_FILE="/usr/local/lib/node_modules/n8n/dist/license.js"
          if [ -f "$LICENSE_FILE" ]; then
            echo "📝 Patching $LICENSE_FILE..."
            # Substituir método isLicensed para retornar sempre true
            sed -i 's/isLicensed(feature) {$/isLicensed(feature) { return true; \/\/ BYPASS/' "$LICENSE_FILE"
            echo "✅ license.js patched"
          else
            echo "⚠️  $LICENSE_FILE not found"
          fi
          
          # Patch 2: Procurar por license-state.js ou similar
          find /usr/local/lib/node_modules -name "*license*.js" -type f 2>/dev/null | while read file; do
            if grep -q "isLicensed.*feature.*{" "$file" 2>/dev/null; then
              echo "📝 Found isLicensed in: $file"
              sed -i 's/isLicensed(feature) {$/isLicensed(feature) { return true; \/\/ BYPASS/' "$file" 2>/dev/null || true
            fi
          done
          
          echo "🎉 All patches applied!"
          PATCH_EOF
          
          chmod +x /tmp/n8n-custom/apply-patches.sh

      - name: 📝 Create custom Dockerfile
        run: |
          cat > /tmp/n8n-custom/Dockerfile << 'DOCKERFILE_EOF'
          ARG N8N_VERSION=latest
          FROM n8nio/n8n:${N8N_VERSION}
          
          USER root
          
          # Copiar e executar script de patch
          COPY apply-patches.sh /tmp/apply-patches.sh
          RUN chmod +x /tmp/apply-patches.sh && \
              /tmp/apply-patches.sh && \
              rm /tmp/apply-patches.sh
          
          USER node
          
          LABEL maintainer="GSConsig"
          LABEL description="N8N with enterprise license bypass"
          LABEL version="${N8N_VERSION}"
          DOCKERFILE_EOF

      - name: 🔧 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🐳 Build Docker image (AMD64 only)
        run: |
          cd /tmp/n8n-custom
          
          IMAGE_TAG="${{ env.GCR_HOSTNAME }}/${{ env.GCP_PROJECT_ID }}/${{ env.IMAGE_NAME }}:${{ env.N8N_VERSION }}"
          IMAGE_TAG_LATEST="${{ env.GCR_HOSTNAME }}/${{ env.GCP_PROJECT_ID }}/${{ env.IMAGE_NAME }}:latest"
          
          echo "🏗️ Building Docker image (linux/amd64): $IMAGE_TAG"
          echo "📦 Base image: n8nio/n8n:${{ env.N8N_VERSION }}"
          
          docker buildx build \
            --platform linux/amd64 \
            --tag "$IMAGE_TAG" \
            --tag "$IMAGE_TAG_LATEST" \
            --build-arg N8N_VERSION="${{ env.N8N_VERSION }}" \
            --file Dockerfile \
            --load \
            .
          
          echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV
          echo "IMAGE_TAG_LATEST=$IMAGE_TAG_LATEST" >> $GITHUB_ENV

      - name: 🚀 Push to GCR
        run: |
          echo "📤 Pushing images to GCR (gcr.io)..."
          
          docker push ${{ env.IMAGE_TAG }}
          docker push ${{ env.IMAGE_TAG_LATEST }}
          
          echo "✅ Images pushed successfully!"
          echo "📦 Image: ${{ env.IMAGE_TAG }}"
          echo "📦 Latest: ${{ env.IMAGE_TAG_LATEST }}"

      - name: 📊 Summary
        run: |
          echo "## 🎉 Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Image Details" >> $GITHUB_STEP_SUMMARY
          echo "- **N8N Version:** ${{ env.N8N_VERSION }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Image Tag:** \`${{ env.IMAGE_TAG }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Latest Tag:** \`${{ env.IMAGE_TAG_LATEST }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Applied Patches" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ License bypass (global)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Quota bypass (unlimited)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ License state bypass" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Pull Command" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "docker pull ${{ env.IMAGE_TAG_LATEST }}" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
